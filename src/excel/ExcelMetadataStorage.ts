import MetadataStorage from '../metadata/MetadataStorage';
import { ExcelColumnOptions, ExcelEntityOptions } from './types';

export default new MetadataStorage<ExcelEntityOptions, ExcelColumnOptions>();
